/*
 * CSE30 FA21 HW5
 * CSE30 username: cs30fa21cXX (TODO: Fill in)
 */
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <getopt.h>
#include "globals.h"

/* Prints the usage message */
void printUsage()
{
    // TODO:
    //   - simple one line of code
    //   - string can be found in globals.h
}

/*
 * Executes the code, accepting a font file, populating the fontBuffer array
 * with the corresponding cBanners, and then printing the given string using the
 * cBanners. A -u flag should print the usage message. A -w flag followed by an int
 * should change the width of the displayBuffer. A -h followed by an int should
 * change the height of the displayBuffer. A -f flag followed by a char
 * should replace the background symbol of the displayBuffer accordingly.
 */
int main(int argc, char **argv)
{

    // TODO:
    //   - handle options using getopt
    //   - how can you read in the cBanners from the font file?
    //   - how should displayBuffer be initialized?
    //   - how should you limit the height and width of displayBuffer if provided in the options?
    //   - how can you write the input into the displayBuffer using cBanners?
    //   - what are the spacing rules between words and between cBanners within a word?
    //   - how can you print the displayBuffer?
    //   - return 0

    return 0;
}
